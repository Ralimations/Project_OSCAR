Project O.S.C.A.R. Documentation Timeline
=======================================

2026-04-08 - Phase 1 foundation started
- Created the baseline OSCAR project structure from the PRD.
- Added .env placeholders for local model paths.
- Added config files for runtime settings, gesture thresholds, prompt rules, and app macros.
- Added core module placeholders for brain, vision, ears, voice, and memory.
- Added actions layer with a threaded ActionController and active-window lookup wrapper.
- Added main orchestrator skeleton with dormant, routing, reasoning, cleanup, and shutdown states.

2026-04-08 - Dormant-state gesture loop established
- Implemented core gesture math in core/eyes.py.
- Added pinch-to-click logic.
- Added two-finger vertical movement to scroll mapping.
- Added head nod and head shake confirmation interpretation in the pure logic path.
- Added cooldown handling so repeated gestures do not flood the action queue.
- Added low-complexity MediaPipe hand detection hook for the live camera path.

2026-04-08 - Non-blocking action execution added
- Implemented queue-based async action dispatch in actions/controller.py.
- Added support for click, scroll, key press, hotkey, app launch, and text entry actions.
- Kept PyAutoGUI optional so the codebase can still load without the package installed.

2026-04-08 - Validation and test scaffolding added
- Added unit tests for gesture interpretation.
- Added unit tests for the threaded action controller.
- Added unit tests for strict JSON extraction and validation in core/brain.py.
- Added tests package initialization for stable unittest discovery.

2026-04-08 - Current phase status before latest change
- Completed phase 1 foundation.
- Remaining gap: wake-word/audio routing is still a stub.
- Remaining gap: app-specific macros are configured but not yet connected to transcript handling.
- Remaining gap: LLM, VLM, Screenpipe, and Piper are intentionally deferred to later phases.

2026-04-08 - Phase 2 in progress
- Starting wake-word event routing and transcript injection scaffolding.
- Connecting active-window macro lookup so simple commands can execute immediately without loading the LLM.
- Preserving the orchestrator pattern so the eyes loop pauses during wake-word handling and resumes after cleanup.

2026-04-08 - Phase 2 routing foundation completed
- Replaced the ears stub with a queue-based transcript event worker in core/ears.py.
- Added transcript injection support so wake-word and STT integration can be tested before the real backend is installed.
- Updated the orchestrator so transcript events pause the vision loop, route commands, and restore dormant mode.
- Added active-window-aware macro resolution for direct command execution from app_macros.json.
- Added a fallback branch that preserves future LLM routing when no direct macro match exists.
- Added tests for macro resolution and transcript delivery.

Current project status
- Phase 1 foundation is complete.
- Phase 2 direct transcript-to-macro routing is complete.
- Real wake-word inference, Faster-Whisper transcription, Screenpipe retrieval, Moondream visual routing, Piper speech, and Llama planning are still pending.
- Runtime test execution is still blocked by the machine's Python launcher access issue.

2026-04-08 - Local Python runtime repaired for development
- Verified the system py launcher remained bound to blocked WindowsApps executables.
- Installed a self-contained Python 3.13.12 runtime into tools/python313.
- Updated python313._pth so the embedded runtime can import the Project_OSCAR workspace.
- Bootstrapped pip inside the local runtime and installed PyYAML.
- Verified the local runtime can execute the test suite successfully.

2026-04-08 - Deterministic transcript routing expanded
- Added core/router.py for cheap offline transcript routing before future LLM fallback.
- Added direct "open <app>" parsing for immediate application launch commands.
- Added direct "open <app> and <task>" parsing that builds ordered action sequences.
- Extended the action controller with sequence and wait actions for multi-step execution.
- Wired the orchestrator to prefer direct routes first, then active-window macros, then future LLM fallback.
- Added tests for sequence execution and direct transcript routing.

Current verified test status
- 12 tests passing with the embedded local runtime.

2026-04-08 - Full PRD module surface added
- Replaced the remaining placeholder modules with concrete offline-local runtime components.
- Expanded core/brain.py with heuristic route classification, strict JSON enforcement, and fallback planning.
- Expanded core/memory.py with schema-tolerant Screenpipe SQLite OCR lookup logic.
- Expanded core/vision.py with tactical screenshot capture and temporary image cleanup.
- Expanded core/voice.py with a Piper subprocess wrapper and text fallback.
- Expanded core/ears.py with openWakeWord and Faster-Whisper loading seams plus transcript injection and audio-file transcription hooks.

2026-04-08 - Orchestrator loop aligned to the PRD
- Wired main.py through direct transcript routing, active-window macro routing, memory retrieval, vision retrieval, brain planning, sanitization, execution, and cleanup.
- Registered speak actions in the action controller so planned speech can be executed through the voice layer.
- Preserved the dormant-to-routing-to-reasoning-to-cleanup state pattern while ensuring Eyes pauses and resumes correctly.
- Kept heavy model backends optional at runtime so the architecture is present even before the final model weights are installed and tuned.

2026-04-08 - Config and dependency surface updated
- Expanded settings.yaml with Screenpipe, Piper, wake-word, Whisper, and model path settings.
- Expanded requirements.txt to include openwakeword, faster-whisper, and Pillow alongside the existing desktop-control and vision dependencies.

2026-04-08 - Test and verification status after PRD wiring
- Added reasoning and memory tests covering route selection, macro priority, JSON sequence validation, and OCR database lookup.
- Fixed a real SQLite connection-lifetime bug in core/memory.py discovered during Windows test execution.
- Verified the expanded codebase with the embedded interpreter using unittest discovery.
- Current result: 17 tests passing.

Current next-day focus
- Install the remaining runtime dependencies into the local embedded interpreter.
- Validate real openWakeWord, Faster-Whisper, MediaPipe, PyAutoGUI, and Piper behavior on the target laptop.
- Debug end-to-end latency, model loading, camera availability, and Windows automation edge cases.

2026-04-08 - Embedded runtime dependencies installed
- Installed setuptools and wheel into the embedded Python to support source-package builds.
- Installed the OSCAR dependency set into tools/python313, including MediaPipe, OpenCV, PyAutoGUI, PyWin32, openwakeword, Faster-Whisper, and Pillow.
- Verified the embedded runtime can import the key PRD-facing packages successfully.
- Re-ran the full test suite after installation and kept the result green.

Current runtime verification
- Embedded interpreter path: tools/python313/python.exe
- Key imports verified: mediapipe, cv2, pyautogui, yaml, win32gui, openwakeword, faster_whisper, PIL
- Current result: 17 tests passing after dependency installation.

2026-04-08 - Usage documentation added
- Added usage_guide.txt with the current control list, supported features, routing behavior, configuration points, and usage examples.
- Documented both what is already implemented and what still requires real-world runtime debugging.

2026-04-08 - Runtime startup hardening
- Patched core/ears.py so missing local wake-word and Whisper model paths no longer trigger invalid initialization or remote model downloads.
- Enforced local-only Faster-Whisper loading to respect the project's offline requirement.
- Patched core/eyes.py so MediaPipe wheels without the legacy solutions API no longer crash the gesture thread; OSCAR now logs a warning and idles that path safely.
- Re-verified the test suite and a short main.py startup smoke run after the hardening changes.

2026-04-08 - Local model folders populated
- Implemented scripts/download_models.py to fetch real local wake-word and Faster-Whisper assets into the models directory.
- Downloaded a working openWakeWord ONNX wake-word model plus required feature models into models/openwakeword.
- Downloaded the Faster-Whisper tiny local model into models/faster-whisper-tiny.
- Verified both wake-word and Whisper loaders initialize successfully from local files.
- Updated .gitignore to keep downloaded model assets out of version control.

2026-04-08 - MediaPipe gesture path migrated to Tasks API
- Replaced the broken legacy mediapipe.solutions hand path with MediaPipe Tasks HandLandmarker usage in core/eyes.py.
- Added a hand landmarker task model path to settings.yaml and wired it into the Eyes runtime.
- Downloaded models/mediapipe/hand_landmarker.task for the installed MediaPipe package.
- Verified the HandLandmarker initializes successfully with the embedded runtime.
- Re-ran the full test suite after the gesture-path migration and kept the result green.

2026-04-08 - Startup diagnostics banner added
- Added startup subsystem diagnostics to main.py so launch logs now report the active interpreter and readiness state of the wake-word, whisper, gesture, vision, voice, and action layers.
- Added small readiness helpers to Eyes, Ears, Vision, and Voice to support explicit startup visibility instead of inferring health from failure logs.
- Re-verified the test suite after adding the diagnostics hooks.

2026-04-08 - Development review windows added
- Added core/dev_overlay.py to render a live tracking window and a sandbox preview window during development.
- Wired Eyes to publish annotated tracking frames and hand landmark positions into the development overlay.
- Wired ActionController to support sandbox-only dry-run execution while still showing action events in the sandbox window.
- Added runtime flags in settings.yaml for development_mode, show_tracking_window, show_sandbox_window, and sandbox_actions_only.
- Re-verified the full test suite after adding the development harness.

2026-04-08 - Startup calibration gate added
- Added a calibration sequence in core/eyes.py that must complete before live gesture actions are dispatched.
- Required startup sequence is now: open hand with 5 visible fingers, nod yes, shake no/disagree, then tilt head for a questioning pose.
- Added a MediaPipe FaceLandmarker path and local face_landmarker.task model to support head calibration.
- Extended the development overlay to display the active calibration step and completed steps.
- Re-verified the suite after the calibration gate changes.

2026-04-08 - Left/right camera mapping corrected
- Added a mirror_camera runtime flag and enabled it by default.
- Updated core/eyes.py to process mirrored frames so user movement matches natural left/right expectations in the tracking and sandbox windows.
- Re-verified the full test suite after the mirror fix.

2026-04-08 - Face tracking visibility and head calibration improved
- Extended the development tracking window to draw face landmark dots and display live pitch, yaw, and roll values.
- Changed yes/no/question calibration from absolute head-angle checks to movement relative to a neutral baseline pose.
- This prevents the old behavior where calibration could default to "yes" from a static face angle instead of a deliberate nod motion.
- Re-verified the full test suite after the head-calibration refinement.

2026-04-08 - Head calibration simplified to yes/no only
- Removed the tilt/question calibration step for now.
- Changed head calibration to require repeated strong motion: two clear vertical swings for yes and two clear horizontal swings for no.
- Kept baseline-relative motion detection so the head steps do not complete from a resting face angle.
- Re-verified the full test suite after the simplification.

2026-04-08 - Live audio ingress scaffold added
- Expanded core/ears.py with an optional live microphone capture loop using sounddevice.
- Added wake-word-triggered utterance capture and temporary WAV transcription flow into the existing Faster-Whisper path.
- Added audio settings to settings.yaml and sounddevice to requirements.txt.
- Added tests for new Ears diagnostics and audio-buffer transcript flow.
- Re-verified the suite after the live audio scaffold changes.

2026-04-08 - Explicit current gaps logged
- Head shake/no calibration remains unreliable and is an open tuning item.
- Wake-word is still using a placeholder built-in model, not a custom OSCAR wake-word model.
- Live audio path is scaffolded but still needs real-device validation, latency tuning, and noise testing.
- Screenpipe, real VLM integration, real LLM/OpenVINO integration, and Piper runtime validation are still pending.

2026-04-08 - Local brain planner strengthened
- Expanded core/brain.py from a minimal fallback into a more capable local structured planner.
- Added support for common command classes such as open app, type/write text, press key, focus address bar, and directional scrolling.
- This improves the transcript -> plan -> action flow immediately without pretending a real Llama backend is already present.
- The true GenAI brain step remains blocked on acquiring and wiring a local Llama/OpenVINO model.

2026-04-08 - Debug calibration skips added
- Added `runtime.calibration_skip_steps` to settings.yaml so specific calibration steps can be bypassed during development.
- The skip list is applied inside the Eyes calibration state machine without disabling the rest of the calibration gate.
- Verified the behavior with a dedicated test and re-ran the full suite successfully.

2026-04-08 - Skipped calibration steps made visible in dev UI
- Updated the sandbox development window to show which calibration steps were skipped by configuration.
- This makes the debug startup state visible without needing to inspect config files or logs during testing.

2026-04-08 - Real brain backend seam added
- Upgraded core/brain.py to support an optional local OpenVINO-backed LLM path while preserving the existing structured fallback planner.
- Added brain diagnostics to startup logging so the runtime now reports whether the configured LLM backend and local model path are actually live.
- Extended settings.yaml and requirements.txt for OpenVINO, Optimum Intel, Transformers, and local LLM generation controls.
- Expanded scripts/download_models.py with a local OpenVINO LLM download path targeting the configured `models/llama` directory.
- Re-ran the full suite after the brain-backend refactor and confirmed all tests still pass.

2026-04-08 - OpenVINO brain runtime installed and verified
- Installed `openvino`, `optimum-intel`, `transformers`, and `sentencepiece` into the embedded Python runtime at `tools/python313`.
- Downloaded the local OpenVINO LLM model `llmware/llama-3.2-1b-instruct-ov` into `models/llama`.
- Verified that core/brain.py now reports the OpenVINO backend as loaded against the local model path.
- Strengthened the prompt template with explicit action vocabulary and JSON-only examples to improve small-model planning reliability.
- Kept the deterministic fallback planner in place so invalid or unusable model output does not break the orchestration path.

2026-04-14 - Deterministic-first routing refactor for low-end laptop performance
- Reworked the orchestrator so transcript requests now take the cheapest reliable route first instead of paying the full screenshot + OCR + model path every time.
- Expanded core/router.py with more direct commands, transcript intent classification, and stronger deterministic routing for common proof-of-concept actions.
- Restored app-macro routing through active-window matching so configured app shortcuts can execute without touching the planner.
- Added transcript-aware fallback planning in core/brain.py so the model path is reserved for ambiguous requests instead of ordinary commands.
- Wired speak actions back into the runtime through core/voice.py and ActionController handler registration.
- Added transcript injection support to core/ears.py so text-driven testing and push-to-talk style flows can use the same orchestrator path.
- Updated the test suite to cover direct transcript routing, macro routing, OCR-memory speaking, planner fallback, and transcript event delivery.
- Verified the refactor with the embedded interpreter: 29 tests passing.

2026-04-14 - Phase 1/2 low-latency tooling and two-hand gesture support
- Expanded the direct command router with more browser/navigation shortcuts so common proof-of-concept commands avoid the planner entirely.
- Added route-level latency logging inside the orchestrator to make it easier to see which path is consuming time on the HP 14s hardware.
- Added `scripts/transcript_harness.py` so OSCAR can be exercised from typed transcript input without waiting on live STT tuning.
- Upgraded the MediaPipe hand path from one tracked hand to configurable multi-hand tracking.
- Added stateful two-hand zoom gesture detection in core/eyes.py using inter-hand distance changes over time.
- Mapped two-hand zoom to configurable hotkeys through settings.yaml instead of introducing a separate heavy gesture action layer.
- Updated tests for router behavior and the new zoom gesture path.

2026-04-14 - Default planner moved to a repo-local small model
- Added a `small_local` profile in `config/settings.yaml` and made it the default active model profile.
- Updated `main.py` so runtime setup and startup downloads respect the active profile's model path instead of relying on the legacy top-level Gemma field.
- Expanded `core/brain.py` to load a local Transformers causal language model directly from a repo folder when the model files are present.
- Updated `scripts/download_models.py` to download `HuggingFaceTB/SmolLM2-360M-Instruct` into `models/smollm2-360m-instruct`.
- Changed the non-Ollama startup bootstrap to skip Ollama pulls and focus on the repo-local asset path.
- Kept deterministic routing first so the small model only handles requests that are not covered by direct commands, app macros, memory lookup, or visual-question routing.
